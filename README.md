# Backyard Pod Scene

Interactive Three.js scene for a 6m x 3.4m backyard pod concept, with orbit controls, first-person POV mode, wall cutaway toggling, and downlight controls.

This repository is intended for GitHub Pages static hosting. The browser entry point is `index.html`, and Three.js is loaded from a pinned CDN import map.

